{
  "name": "fetch-proxy-lambda",
  "version": "1.0.0",
  "author": "K",
  "main": "lambda.js",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --noEmit && esbuild src/fetch-proxy-lambda.ts --bundle --platform=node --target=node22 --sourcemap=inline --minify --outfile=build/lambda.js",
    "dist": "rimraf dist/lambda.zip && shx mkdir -p dist && cd build && bestzip ../dist/lambda.zip lambda.js"
  },
  "type": "module",
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.33.0",
    "@js-joda/core": "^6.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "22.19.17",
    "bestzip": "^2.2.3",
    "esbuild": "^0.28.0",
    "rimraf": "^6.1.3",
    "shx": "^0.4.0",
    "typescript": "^6.0.3"
  }
}
